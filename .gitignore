# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Env / secrets
.env
*.local

# 本地 SQLite 记忆库与 WAL 旁车文件（含用户对话，绝不入库）
data/
*.db
*.db-wal
*.db-shm

# 本地 AI 协作者约定，只留本机、不推远端
AGENTS.md

# Test / tool caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# 由 scripts/build_walkthrough_docx.py 生成的讲义产物（需要时本地构建）
docs/exports/

# 杭州参赛材料截图（含个人信息，永不入库；目录由用户录材料时本地创建）
docs/competitions/hangzhou/evidence/

# host_e 销售 CRM 演示数据（运行时生成，不入库）
examples/host_e_sales_crm/crm_data.json

# 路由自校准 benchmark 生成物（脚本一键复现，不入库；定稿图见 docs/assets/）
benchmark_out/

# 本地 bge-m3 向量模型（约 560MB，不入库；下载说明见 models/README.md）
models/*
!models/README.md
