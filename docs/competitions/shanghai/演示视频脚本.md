# 上海开源赛演示视频脚本（3–4 分钟，开源 AI 工具赛道）

> 目标：让评审在 3–4 分钟内相信这是一个**可运行、可复现、治理规范的开源 Agent 框架**，而不是又一个"打开即用"的产品演示。
> 素材工程：`C:\Users\sheng\Videos\屏幕录制\.edit_work\`（杭州版同源，复用 ffmpeg 卡片/字幕条管线）。
> 与杭州版的差异：杭州版卖"数字员工产品"，上海版卖"**内嵌内核 + 多宿主 + 开源工程**"。

## 一、素材复用对照

| 素材 | 内容 | 上海版用法 |
|---|---|---|
| 段2-1 / 段2-2 | 宿主声明能力（普通函数）、`AgentCore.auto()` 自动发现 | 复用，证明"宿主声明、内核适配" |
| 段3 | Render `/health`、`/v1/tools`（本地工具 + 远程 MCP 工具，清单以实时返回为准） | 复用，证明在线可验证 |
| 段4 | 一条 `curl` 跑 `/v1/agent/run`，自适应路由 react | 复用，证明可复现 |
| 段5-1 | 业务本体 standalone 菜单，未接入 AI 也完整 | 复用，证明"不绑架业务" |
| 段5-2 | 同一份业务代码自动发现 12 个工具、写操作问人 | 复用，证明零改造嵌入 |
| 段6 | 网页工作台复合任务闭环（73.5s） | 复用但**剪到约 50s**，去掉最细的操作 |
| **段7（新录）** | 开源工程与治理 | **必须新录，约 45–60s** |

## 二、需要新录的"段7：开源工程与治理"（约 45–60 秒）

按顺序录一个窗口，每段 8–12 秒，鼠标缓慢移动：

1. **GitHub 仓库主页**：README 徽章、目录结构、`src/yai_core/` 内核目录。
2. **`pyproject.toml`**：特写 `dependencies = []`（内核零硬依赖），可选 extras 分组。
3. **终端跑测试**：`uv run pytest`，结尾停在 **241 passed**（全程不依赖网络/Key）。
4. **`uv run ruff check src tests examples scripts`**：All checks passed。
5. **（强烈建议）能力缺口闭环**：优先用 host_e 网页工作台问"明天去广州拜访要带伞吗"，拍到完整序列 `capability_missing（琥珀）→ tool_discovered（绿）→ 新能力授权（黄）→ 调用出结果`（与作品介绍 PDF 图 2 同源，是技术创新最直观的一镜）；离线/无 Key 时用 `uv run python examples/host_f_discovery/run.py` 拍确定性序列 `capability_missing → tool_discovered → get_weather → 结果`。
6. **GitHub Actions 页**：四矩阵（Ubuntu 3.11/3.12/3.13 + Windows 3.13）全绿。
7. **`ROADMAP.md` + `docs/comparison.md`**：快速滚动，展示路线与诚实对比。

> 录制注意：浏览器放大到 125–150%；终端字号调大；不要出现 .env、Key、真实姓名（页面署名只有 GitHub: Gi-Tuu / 队名 YAI）。

## 三、分镜与卡片（目标时长约 3 分 20 秒）

| 顺序 | 画面 | 时长 | 字幕卡 / 段落卡 |
|---|---|---|---|
| 1 | 片头 | 5s | 开源 AI 工具赛道 · **YAI Agent Core**：进程内可内嵌的自适应 Agent 内核 |
| 2 | 段落卡 | 2.5s | 第一部分 · 宿主声明能力，内核自适应完成其余 |
| 3 | 段2-1 + 段2-2 | 14.7s | 复用原字幕 cap_2a / cap_2b |
| 4 | 段落卡 | 2.5s | 第二部分 · 在线可验证：公网 API + 事件流 |
| 5 | 段3 | 10.1s | 复用 cap_3a / cap_3b |
| 6 | 段4 | 15.0s | 复用 cap_4a / cap_4b |
| 7 | 段落卡 | 2.5s | 第三部分 · 存量软件零改造：业务本体不导入 AI |
| 8 | 段5-1 | 21.6s | 复用 cap_51 |
| 9 | 段5-2 | 27.3s（1.3x） | 复用 cap_52 |
| 10 | 段落卡 | 2.5s | 第四部分 · 嵌入即得：同一内核自适应换岗 |
| 11 | 段6（剪短） | ~50s | 复用 cap_6a–g，片尾授权/日报部分保留 |
| 12 | 段落卡 | 2.5s | 第五部分 · 开源工程与治理 |
| 13 | **段7（新录）** | 45–60s | 新字幕（见下） |
| 14 | 片尾 | 5s | 内核零硬依赖 · 241 项离线测试 · 六契约 SPI · 四矩阵 CI · GitHub: Gi-Tuu/yai-agent-core |

段7 新字幕（录制后按实际时间微调）：
- `cap_7a`：仓库主页与内核目录，SPI 六契约清晰分层
- `cap_7b`：pyproject.toml 的 dependencies 为空，能力全部可选 extra
- `cap_7c`：241 项离线测试，不依赖网络与 API Key
- `cap_7d`：ruff 静态检查 + GitHub Actions 四矩阵全绿
- `cap_7e`：能力缺口最小闭环——缺口感知（琥珀）→ 按需发现（绿）→ 授权（黄）→ 本轮即用
- `cap_7f`：ROADMAP 与对比文档，规划与边界公开透明

## 四、构建产物

- 新卡片：`make_assets_shanghai.py` 生成 `sh_*.png`（不覆盖杭州版卡片）。
- 构建脚本：`build_shanghai.ps1`，段7 就位后一键出片；段6 剪短点与段7 字幕时间在录完后填实。
- 输出：`YAI_上海开源赛_演示_1080p.mp4`（沿用 2560×1536 母版，导出缩放 1920×1152）。
- 视频在线链接：打 tag 后挂 GitHub Release（与杭州版同仓不同 Release），报名邮件只给链接。

## 五、口径红线

- 不说"多智能体 / RAG / 向量 / 自进化"；能力发现只讲"进程内静态目录最小闭环已落地"，MCP/OpenAPI 动态发现与工具组合明确标注为"路线中"。
- 不说"支持任意软件"；说"Python 软件通过声明能力接入"。
- 演示数据均为虚构；不出现 .env、Key、真实姓名。
- 数字与仓库一致：241 测试、6 宿主、在线实例的本地工具 + MCP 工具（以 `/v1/tools` 实时返回为准）、SPI 六契约、内核零硬依赖。
